<div class="row">
    <div class="col col-12 font-66">
        <h1><em><strong>{{destination.name}}</strong></em></h1>
    </div>

    <div class="col col-12">
        <img class="img-fluid wide-screen" src={{destination.image}} alt="{{destination.name}} image">
    </div>

    <div class="col col-12">
        <p>{{destination.description}}</p>
    </div>
</div>